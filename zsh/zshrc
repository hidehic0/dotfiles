SPLIT_FILES="/home/hidehico/.config/zsh/zsh.d"

# sheldonの設定
eval "$(sheldon source)"

# hist settings
source "$SPLIT_FILES/hist.zsh"

# p10k settings
source "$SPLIT_FILES/p10k.zsh"


# rust path
. "$HOME/.cargo/env"

# alias import
source "$SPLIT_FILES/alias.zsh"

# function import
source "$SPLIT_FILES/func.zsh"

